# Superuser Credentials

| Field    | Value           |
|----------|-----------------|
| Email    | admin@coop.local |
| Username | admin           |
| Password | Admin1234!      |
| Role     | admin           |

These are the default values from `.env.example` (`SEED_SUPERUSER_*`).
Override them in your `.env` before the first `docker-compose up`.
