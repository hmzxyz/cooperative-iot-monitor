# Sprints

## Completed

- Dockerized the stack with `backend`, `frontend`, `mosquitto`, `postgres`, and `nodered`.
- Standardized the database flow around `iot_monitor.sensor_readings`.
- Reworked the dashboard to read machine bundles from the backend API.

## In Progress

- Cleaning legacy sprint material and doc sprawl.
- Tightening the dashboard layout and motion system.

## Next

- Finish doc archive cleanup.
- Keep Node-RED and simulator payloads aligned with the current bundle schema.
- Preserve the Docker-first run path as the canonical startup method.
